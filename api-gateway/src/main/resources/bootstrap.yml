spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://config:password@localhost:8088

---
spring:
  profiles:
    - native
  cloud:
    config:
      enabled: false

