spring:
  application:
    name: discovery-service
  cloud:
    config:
      enabled: true
      username: config
      password: password
      uri: http://localhost:8088
  zipkin.baseUrl: http://zipkin-service/